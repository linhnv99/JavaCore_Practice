package abstract_interface;

public class Main {
	
	
	/*
	 * =========SỰ KHÁC NHAU GIỮA abstract và interface=========
	 * 
	 * Abstract:
	 * 	+ có hoặc ko có phương thức abstract
	 * 	+ không hỗ trợ đa kế thừa
	 * 	+ các biến là final, non-final, static và non-static
	 * 	+ abstract có thể triển khai phương thức của interface
	 * 	
	 * Interface:
	 * 	+ tất cả phương thức đều là abstract method mặc định sẽ thêm public ở đầu
	 * 	+ hỗ trợ đa kế thừa
	 * 	+ chỉ hỗ trợ biến final và static
	 * 	+ interface KHÔNG thể triển khai phương thức của abstract
	 * 
	 * =============================
	 * => Cả abstract và interface đều k thể khởi tạo bằng từ khóa new
	 * 
	 * 
	 * 
	 * 
	 * */
}
